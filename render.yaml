services:
  - type: web
    name: Valence-backend
    runtime: python
    rootDir: compfinder/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT